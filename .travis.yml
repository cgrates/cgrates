language: go

before_install: sudo apt-get install bzr -qq

branches:
  only: master

notifications:
  irc:
    channels:
      - "irc.freenode.net#cgrates"
    on_success: change
    on_failure: always
  email:
    on_success: change
    on_failure: always

after_script: ./test.sh
