language: go

branches:
  only: master

notifications:
  irc:
    channels:
      - "irc.freenode.net#cgrates"
    on_success: change
    on_failure: always
  email:
    on_success: change
    on_failure: always

after_script:
    ./build.sh && ./test.sh
