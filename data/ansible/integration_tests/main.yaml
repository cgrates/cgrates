---
- hosts: all
  roles:
    - ../roles/install_rabbitmq
    - ../roles/kafka
    - ../roles/install_mongodb
    - ../roles/postgresql
    - role: ../roles/go
      when: install_go | default(true)
    - ../roles/cgrates
