---
- hosts: all
  roles:
    - ../roles/rabbitmq
    - ../roles/kafka
    - ../roles/mongodb
    - ../roles/postgresql
    - role: ../roles/go
      when: install_go | default(true)
    - ../roles/cgrates
